cache:
  # File stat cache (persisted to .transfs_getattr_cache.pkl)
  stat_cache_enabled: false
  stat_cache_save_interval: 5.0
  # Transform caches (in-memory runtime performance)
  transform_output_size_cache_enabled: true
  transform_pipeline_cache_enabled: true
  # Readdir optimization (in-memory runtime performance)
  readdir_direntry_cache_enabled: true
  readdir_skip_cache_lookup_with_direntry: true
cache_warmer:
  batch_size: 10
  check_interval_seconds: 5
  cpu_threshold_percent: 20.0
  enabled: false
  io_threshold_percent: 20.0
  max_depth: 12
config_sets:
  active_client_config: default
  active_source_config: default
database:
  auto_sync: true
  enabled: true
  mode: enabled
  sync_on_startup: true
  # PostgreSQL connection (environment variables override these)
  host: postgres
  port: 5432
  name: transfs
  user: transfs
  password: transfs_pass
  pool_size: 20
  max_overflow: 10
filestore: /mnt/filestorefs
mame:
  # MAME Software List downloader configuration
  hash_repo_url: https://raw.githubusercontent.com/mamedev/mame/master/hash
  hash_repo_branch: master
  # IMPORTANT: archive_base_url must point to an Internet Archive collection containing MAME Software List files
  # The files must match the filenames in the hash XML files (e.g., 747(bugbyte).hq.uef for Atom cassettes)
  # Common collections:
  #   - MAME Software Lists (cassettes, floppies): Search archive.org for "MAME Software List"
  #   - MAME ROMs (arcade): Not suitable for Software Lists
  # Example URL format: https://archive.org/download/collection-name/archive-name
  # Internet Archive allows accessing files inside archives: /archive.zip/filename
  archive_base_url: https://archive.org/download/mame-merged/mame-merged
  download_root: /mnt/filestorefs/Downloads/MAME
  verify_checksums: true
  max_concurrent_downloads: 3
  cache_hash_files: true
mountpoint: /mnt/transfs
ssl_ignore_hosts:
- arcarc.nl
- releases.prodos8.com
ui:
  advanced_options: true
  show_real_path_tooltips: true
web_api:
  host: ''
  port: 8000
zaparoo:
  clients:
  - enabled: true
    host: 192.168.95.78
    mount_path: /media/fat/cifs
    name: MiSTer
    port: 7497
